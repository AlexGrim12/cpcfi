#include <bits/stdc++.h>

using namespace std;

void solve()
{
    // Add your solution here
    int n;
    cin >> n;
    int c[n];
    string s;
    for (int i = 0; i < n; i++)
    {
        cin >> c[i];
        cin >> s;
        for (int j = 0; j < s.lenght; j++)
        {
            if ()
            {
                /* code */
            }
        }
    }
}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int tc = 1;
    // cin >> tc;
    for (int t = 1; t <= tc; t++)
    {
        // cout << "Case #" << t << ": ";
        solve();
    }
}
