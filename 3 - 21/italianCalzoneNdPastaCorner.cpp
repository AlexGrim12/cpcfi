#include <bits/stdc++.h>

using namespace std;

void solve()
{
    // Add your solution here
    int r, c;
    cin >> r >> c;
    vector<vector<int>> vec;
    for (int i = 0; i < r; i++)
    {
        for (int j = 0; j < c; j++)
        {
            cin >> tmp;
            vec
        }
        
    }
    
}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int tc = 1;
    // cin >> tc;
    for (int t = 1; t <= tc; t++)
    {
        // cout << "Case #" << t << ": ";
        solve();
    }
}
